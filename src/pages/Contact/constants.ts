import { HousingRegime, HousingType } from '../../common/types/contact';

export const HOUSING_TYPES: string[] = Object.values(HousingType);
export const HOUSING_REGIME: string[] = Object.values(HousingRegime);
