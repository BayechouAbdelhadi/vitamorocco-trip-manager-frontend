import { Page } from '../../common/components/Page/Page';

export const Tours = (): JSX.Element => {
    const ToursText = 'Tours';

    return (
        <Page description={ToursText} keywords={ToursText} title={ToursText}>
            Tours
        </Page>
    );
};
