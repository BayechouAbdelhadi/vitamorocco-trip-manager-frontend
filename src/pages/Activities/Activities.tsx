import { Page } from '../../common/components/Page/Page';

export const Activities = (): JSX.Element => {
    const ActivitiesText = 'Activities';

    return (
        <Page description={ActivitiesText} keywords={ActivitiesText} title={ActivitiesText}>
            Activities
        </Page>
    );
};
