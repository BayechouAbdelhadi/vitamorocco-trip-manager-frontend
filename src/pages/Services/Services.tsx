import { Page } from '../../common/components/Page/Page';

export const Services = (): JSX.Element => {
    const ServicesText = 'Services';

    return (
        <Page description={ServicesText} title={ServicesText}>
            Services
        </Page>
    );
};
