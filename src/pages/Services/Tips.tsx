import { Page } from '../../common/components/Page/Page';

export const Tips = (): JSX.Element => {
    const TipsText = 'Tips';

    return (
        <Page description={TipsText} keywords={TipsText} title={TipsText}>
            Tips
        </Page>
    );
};
